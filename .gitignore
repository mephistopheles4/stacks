node_modules/
dist/
.astro/
.cache/

# Build artifact — always regenerable from the vault, never hand-edited (CLAUDE.md invariant 1)
library.json

# Screenshots are regenerable via `pnpm smoke:render`; keep binaries out of the repo
artifacts/

.env
.env.*
!.env.example

*.log
.DS_Store
Thumbs.db
